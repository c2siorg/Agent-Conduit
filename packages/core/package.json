{
  "name": "@conduit/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types, JWT/grant models, and canonical schema — the single source of truth all workspaces import.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  }
}
