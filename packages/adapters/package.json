{
  "name": "@conduit/adapters",
  "version": "0.1.0",
  "private": true,
  "description": "ToolAdapter interface (Adapter + Registry) normalizing MCP / OpenAPI / CLI schemas into Conduit's canonical form.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@conduit/core": "*"
  }
}
