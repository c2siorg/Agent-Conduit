{
  "name": "@conduit/crypto",
  "version": "0.1.0",
  "private": true,
  "description": "Ed25519 keypair generation + EdDSA JWT signing/verification + RFC 7638 thumbprints — node:crypto ONLY.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@conduit/core": "*"
  }
}
