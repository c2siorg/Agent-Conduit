/**
 * conduit-client — the AAP Client SDK (published to npm).
 * Single-provider by design; provider-discovery + Directory tools are intentionally out of scope for v1.
 */
export * from './conduitClient.js';
