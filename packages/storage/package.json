{
  "name": "@conduit/storage",
  "version": "0.1.0",
  "private": true,
  "description": "StorageDriver interface (Strategy + Repository + Factory) and pluggable backends: postgres (default), mysql.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@conduit/core": "*",
    "@conduit/crypto": "*",
    "node-pg-migrate": "^7.6.1",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6"
  }
}
