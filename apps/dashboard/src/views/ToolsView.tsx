/**
 * Tool & Schema Router — registered tools, adapter type (MCP / OpenAPI / CLI), and token-cost telemetry.
 * @remarks Scaffold.
 */
export function ToolsView(): JSX.Element {
  return (
    <section className="view">
      <h1>Tool &amp; Schema Router</h1>
      <p className="muted">Scaffold — registered tools, adapter type, and token-cost sparklines render here.</p>
    </section>
  );
}
